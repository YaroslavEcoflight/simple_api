
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    restart: unless-stopped
    environment:
      - HOST=${HOST}
      - PORT=${PORT}
      - VERSION=${VERSION}
